# Varialble 

# Variables are used to store the vlue 

# For example: 

name = input("What is your Name?")

print(name)

# in the above name holds the value whaterver the user enter, and later we can print the value.

# the variable  name can be changed also

# For example: 

name = "Ananad"

print(name)

# At first the user enter the name: Pratik and we print that name however later we changed that name

# name to Ananad and then we print the  same variable name, the value has changed 

# Suppose we want to count the characters of something the use input, then we can do this by using len function 

# For example: 

# I want to count the numbers of characters of my name

print(len(input("What is your name?")))

# we can also split the above code with the use of variables and make it more readable 

userName = input("What is your name?")

lenght_of_UserName = len(userName)

print(lenght_of_UserName)